{{#each collections as |collection| }}
<section class="card">
  <a href="/apartmentcollection/{{id}}">
    <header>{{title}}</header>
    {{!-- <img src="/images/{{paintings.[0].filename}}" alt="{{paintings.[0].description}}"> --}}
  </a>
</section>
{{/each}}